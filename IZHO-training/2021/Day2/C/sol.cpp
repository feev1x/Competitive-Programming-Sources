/**
 *    author:  feev1x
 *    created: 27.12.2024 13:03:04
**/
#include <bits/stdc++.h>

using i64 = long long;

int main() {
  std::ios_base::sync_with_stdio(false);
  std::cin.tie(nullptr);
  int n, q; std::cin >> n >> q;
  std::vector<int> a(n), b(n), p(n + 1);
  for (int i = 1; i <= n; ++i) {
    std::cin >> p[i];
  }
  for (int i = 1; i < n; ++i) {
    std::cin >> a[i] >> b[i];
  }
LEA
  return 0;
}
