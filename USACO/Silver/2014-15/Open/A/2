/**
 *    author:  feev1x
 *    created: 28.11.2024 22:25:22
**/
#include <bits/stdc++.h>

using i64 = long long;

int main() {
  int n; scanf("%d", &n);
  std::map<char, int> mp;
  for (int i = 0; i < n; ++i) {
    char ch; int n; scanf(" %c %d", &ch, &n);
    printf("%c\n", ch);
  }
  return 0;
}
