/**
 *    author:  feev1x
 *    created: 19.12.2024 17:56:36
**/
#include <bits/stdc++.h>

using i64 = long long;

int main() {
  std::ios_base::sync_with_stdio(false);
  std::cin.tie(nullptr);
  int n; std::cin >> n;
  std::vector<int> a(n), b(n);
  int x = 0;
  for (int i = 0; i < n; ++i) {
    std::cin >> a[i] >> b[i];
  }
  std::vector<int> a();
  return 0;
}
