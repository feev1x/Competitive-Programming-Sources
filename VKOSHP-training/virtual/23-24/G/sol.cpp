#include <bits/stdc++.h>

int main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);

    int n; std::cin >> n;

    auto Ask = [&](int64_t d) {
        std::cout << "? " << d << std::endl;

        int sz; std::cin >> sz;

        return sz;
    };

    
    return 0;
}
