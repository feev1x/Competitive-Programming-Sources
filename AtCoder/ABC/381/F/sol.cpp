/**
 *    author:  feev1x
 *    created: 05.12.2024 10:12:40
**/
#include <bits/stdc++.h>

using i64 = long long;

int main() {
  int n; scanf("%d", &n);
  std::vector<std::vector<int>> pos(21);
  for (int i = 0; i < n; ++i) {
    int u; scanf("%d", &u);
    pos[u].emplace_back(i);
  }
  for (int i 
  return 0;
}
