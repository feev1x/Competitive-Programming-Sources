/**
 *    author:  feev1x
 *    created: 31.03.2025 13:12:45
**/
#include <bits/stdc++.h>

int main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);
    
    int n, m; std::cin >> n >> m;

    std::vector<int> a(n);
    for (auto &u: a) {
        std::cin >> u;
    }

    int ans = 0;
    std::deque<int> dq;
    for (int i = 0; i < n; ++i) {

        
    }
    return 0;
}
